[project]
name = "$1"
version = "$2"
description = "$3"
authors = [
    { name = "$4" }
]
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }

# Welche Pakete installiert werden sollen
dependencies = []

# Optional: falls du Skripte/CLI anbieten willst
[project.scripts]
$1 = "$1.app:main"

[tool.setuptools.package-data] 
$1 = ["resources/*"]

# Build-System (Standard für moderne Python-Projekte)
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
